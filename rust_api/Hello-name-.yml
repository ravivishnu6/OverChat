info:
  name: Hello<name>
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/greet
  body:
    type: json
    data: |-
      {
        "name": "Dev"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
