info:
  name: Update Name
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/name
  body:
    type: json
    data: |-
      {
        "name": "Dev"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
